{
  "name": "pluggedin-solar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "posts": "node scripts/build-posts.js",
    "sitemap": "node scripts/build-sitemap.js",
    "prebuild": "npm run posts && npm run sitemap",
    "dev": "npm run posts && npm run sitemap && vite",
    "build": "npm run posts && npm run sitemap && vite-react-ssg build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0",
    "leaflet": "^1.9.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.28.0",
    "vite-react-ssg": "^0.9.1-beta.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "marked": "^15.0.0",
    "vite": "^6.0.0"
  }
}
