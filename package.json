{
  "name": "pluggedin-solar",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "posts": "node scripts/build-posts.js",
    "sitemap": "node scripts/build-sitemap.js",
    "dev": "npm run posts && npm run sitemap && vite",
    "build": "npm run posts && npm run sitemap && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-helmet-async": "^3.0.0",
    "react-router-dom": "^6.28.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "marked": "^15.0.0",
    "vite": "^6.0.0"
  }
}
